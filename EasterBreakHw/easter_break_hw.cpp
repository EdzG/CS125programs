//Eder Guerrero
//27 March 2021
//Program that reads a set of points and produces a set of points 
//based on a 2D transformation. 
/*
#include <iostream>
#include <cmath>

using namespace std;

void calcAns(int oldX, int oldY, double angle, int transX, int transY, double& newX, double& newY)
{
	newX = (oldX * cos(angle)) - (oldY * sin(angle)) + transX;
	newY = (oldX * sin(angle)) + (oldY * cos(angle)) + transY;
}

void Arrays(int rows, int cols)
{
	for (int i = 0; i < rows; i++)
	{
		for (int j = 0; j < cols; j++)
		{
			cin >> array[i][j];
		}
	}
}


int main()
{
	double newX, newY;
	int numOfPoints, col;
	
	cin >> numOfPoints;
	degrees = (degrees * 3.14) / 180;

	int* points = new int[numOfPoints];


	delete[]points;
	points = nullptr;

	return 0;
}
*/